{
  "name": "appultron",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev:clean && concurrently --kill-others-on-fail --names API,WEB --prefix-colors cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:clean": "kill-port 4000 5173",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
